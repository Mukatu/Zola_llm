# Surface de déploiement : box (client) | cortex (cabinet) — deux apps isolées
NEXT_PUBLIC_SURFACE=box
# Base de l'API ZolaOS (/v1)
NEXT_PUBLIC_API_BASE=http://localhost:8000
# Jeton d'auth (Bearer) pour /v1/query (capacités génératives). Optionnel en dev.
NEXT_PUBLIC_API_TOKEN=
